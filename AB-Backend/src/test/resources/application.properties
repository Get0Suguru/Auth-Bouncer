# Dummy values for tests ? nothing here is real, nothing here is used for real connections
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=test
spring.datasource.password=test

jwt.secret=test-secret-key-for-unit-tests-only-1234567890
jwt.access-token.expiration=900000
jwt.refresh-token.expiration=604800000

google.clientId=test-client-id
google.clientSecret=test-client-secret
google.redirectUri=http://localhost/callback

github.clientId=test-client-id
github.clientSecret=test-client-secret
github.redirectUri=http://localhost/callback

spring.mail.host=localhost
spring.mail.port=25
spring.mail.username=test@test.com
spring.mail.password=test

spring.data.redis.host=localhost
spring.data.redis.port=6379

rate.limiting.max.requests=100